# Tests run against in-memory H2 (PostgreSQL mode) so no DB is needed.
spring.datasource.url=jdbc:h2:mem:core;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
